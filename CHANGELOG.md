# 更新日志 / Changelog

Keep a Changelog 风格。发布 tag v* 时,CI 自动提取对应版本章节作为 GitHub Release 说明。
版本号与 `custom_components/lechange_door_lock/manifest.json` 一致;**版本号只在发布时确定**,
日常新功能/修复统一并入 [未发布],发布时再固化。

---

## [未发布]

(无)

## [v1.7.0] - 2026-09-07

### 变更
- **物模型双端点兼容**:新设备优先 `GetModelAndPlatFormJsonByProduct`(模型更全,
  能力门控可建更多实体);老设备失败/空体自动回落 `QueryModelInfo` 并按产品记忆。
- **热更新协议对齐**:写属性切 `SetIotProperties`(仅 11001 回落旧端点);读属性优先
  `GetIotProperties`(缺 `properties` 键或 11001 自动回落)。MQTT 层硬拒写类 API,
  定位纯订阅推送。

### 修复
- **两步式临时密码不被服务端认可**:`effectPeriod` 改逐日 7 条、时间格式 `T000000`;
  Add 登记对齐 App(`tempKey` 原样回传、生效/过期时间用服务端签发值);
  `usagePeriod` 按设备时区(UTC+8)取"今天",UTC 主机不再错位一天。
- **童锁状态"关↔开"跳动**:属性合并以上轮已提交数据为基线,防缺键闪跳;
  写入后 90s 窗口抑制迟到旧值;未知值显示"不可用"而非"关"。
- **童锁载体纠偏**:只认 `sdl_inOpenDoorModel`(171700)单一真源;写入走官方
  `SetProperties`,不唤醒设备,写入后状态即时刷新。
- **稳健性**:并发重登串行化、卸载顺序、事件去重键、多账号会话匹配、
  443 端口网关判断、nonce 改安全随机、DAV 解密伪命中、RTSV 关闭超时。

### 新增
- **开门设置页**:按"型号声明 ∩ 实测返回 ∩ 可写"自动创建开关/选择/只读传感器
  (自动上锁、门未关提醒、提示音音量、强制锁定等);高危属性只读展示;
  写入 int 铁律 + 休眠唤醒重试;新增"唤醒设备"按钮。

---

## [v1.6.5] - 2026-09-05

### 变更
- 临时密码生成改两步式(App 同链路,服务端签发),设备离线回落客户端自产。
- 生效星期改为系统自动分配(每天),移除星期选择实体。

### 新增
- "最新告警"更名"告警信息",属性结构化为字典列表(more-info 小表渲染)。
- 属性展示文本国际化(zh-Hans / en)。
- 工作模式 select + powerMode 传感器。
- 临时密码列表重构为单属性字典列表(上限 20)。

### 修复
- keyId 收敛 8 位内(9 位导致 App 无法删除)。
- 删除对齐 App 包并核验结果,失败发 `snapkey_delete_failed` 事件。
- "次数"改读 `number` 字段;"最近开门方式/时间"排序与解析修正。
- MQTT 优雅释放会话,修复重连被拒风暴("0 bytes read" 刷屏)。

## [v1.6.2] - 2026-09-05

### 修复
- **同账号多设备互扰**:共享一条 MQTT 连接与会话,推送按 deviceId 分发。
- **休眠期数据被清零**:刷新拿不到的字段一律继承上次值;写入后乐观更新。
- **传感器/服务全消失**:sensor 平台构造函数缺失、服务注册 NameError。
- **升级即换终端强制重新授权**:terminal_id 安装级持久化,12112 自动走重新认证流程。
- **MQTT 稳定性**:TLS 上下文移出事件循环,退避 5s→10 分钟封顶。
- 其余:门外截图云端兜底、童锁严格布尔判定、配置保存不丢留空字段、
  services.yaml/翻译语法、新版 HA 弃用 API 兼容。

### 新增
- **休眠期数据源**:云端属性缓存 + 设备列表快照补拉,休眠期仍显示真实值。
- **摄像头自动取图总开关**(默认开):关闭后仅返回缓存帧,解决常驻摄像头卡片耗电。
- **条目级重新加载 + `reload_data` / `dump_diagnostics` / `set_credentials` 服务**。
- 开门记录双通道合并;临时密码数量取真实条数。

### UI 增强
- 门状态文案"门已锁/门未锁";可控 lock 实体移除,开锁用远程开门按钮或服务。
- 集成配置(options)全面汉化;select 选项全部汉化;移除"呼叫转接"开关。

## [v1.6.1] - 2026-09-04

### 修复
- 添加设备最后一步 "Unknown error occurred":登录返回键名兼容回退
  (`internal_username` → `username`),字段缺键整体兜底。

### UI 增强
- GT4 滑块步骤显示完整可点击链接(外网 → 内网 → HA Cloud 逐级回退)。

### 其他
- hacs.json 恢复最低 HA 版本约束(2024.1.0);CI 测试依赖补 voluptuous。

## [v1.6.0] - 2026-09-04

### 会话信任模型
- sid 持久、token 继承 sid 登录史;单账号单活跃 token,被顶一端自动重登自愈。

### 新增
- **自主续期登录链**:密码 GetToken → Login 激活,两个请求完成续期,零人工介入。
- **GT4 本地滑块验证**:挂在 HA 自身 HTTP 端口,不再要求"去乐橙 App 完成验证"。
- AK 身份接口支持 default 前缀;OEM AK/SK 改环境变量提供。
- 错误码 12114(需 GT4)、12112(需终端授信)定向处理。

### 修复
- GrantingCredit 按线上协议校准(验证码原码直传,无 CheckValidCode 中间步)。

## [v1.5.0] - 2026-09-04

### 新增
- **云端实时预览**:RTSV1 取流录制,主码流 H265 优先,可开 OSD 时间戳/通道名。
- **门外截图**:camera 快照 + 服务,双摄组合(左右/上下/单摄),60s 节流。
- **告警图**:DAV 容器解密(安全码)输出原厂 JPEG。
- **MQTT 实时通道**:TLS 8883,控制 MQTT 优先 → 云 API 兜底,属性推送实时更新实体。
- 本地通道地址(LAN snapshot.cgi 优先,自动回退云端);内置 OSD 思源黑体;
  UA 设备特征池;ffmpeg 系统依赖。

### 修复
- 12002 纳入自动重登码表,明确"后登者顶前者,被顶者自愈"共存语义。
- client-ua 迁移线上安卓特征;terminalId 迁移 App 同款标准 UUID。

## [1.4.0] - 2026-09-03

### 新增
- 配置向导支持**短信验证码登录**;密码失败按错误码定向提示(需短信/需人机验证)。

### 修复
- 临时密码改走 App 云消息 API(SmartLockSecretAdd/ListV2/Delete),
  设备休眠可用、不触发验证码。
- 消息域 apiver 分域(iot.message.* = V10.2.2)。
- 云侧告警接入:休眠照常返回,新告警触发 `lechange_door_lock_event`。

### 其他
- "唤醒设备"改为"获取门外截图"(拉取摄像头唤醒 + Dahua CGI 抓拍)。

## [1.3.0] - 2026-09-03

### 新增
- 临时密码配置实体化:number(次数/天数)、select(生效星期)、text(名称)、
  time(起止);生成/刷新按钮;临时密码数量与最近开门方式/时间传感器;
  主/辅摄像头通道在线二进制传感器。

## [1.2.0] - 2026-09-03

### 协议与核心
- **切换为客户端私有云协议**(账号+密码 → x-pcs 双签名 → 区域网关)。
- 会话自动续期持久化;型号定义 identifier↔ref 递归解码。

### 新增功能
- 门锁实体 + 远程开锁;童锁/呼叫转接/触屏开门开关;每通道摄像头实体。
- 对话服务(应答/拒绝/挂断门铃、语音回复读写);门锁/摄像机双电池等传感器;
  临时密码与开门记录事件。

### 修复
- 登录后 token/内部用户名未解析导致会话失效与重登死锁;登录密钥派生。

### 质量与合规
- 单元测试 50 例;CI(HACS 校验 + pytest + tag 发布 ZIP);MIT LICENSE、
  README 免责声明;统一更新日志;公开文件脱敏。

> v1.1.0 为开发中间版本(未发布),其内容已并入本版本,不再单独记录。

## [1.0.1] - 2026-03-11

- 添加开门记录(旧开放平台接口版本)。

## [1.0.0] - 2026-03-10

- 首次发布(旧开放平台接口版本:设备状态、电量、远程开门、唤醒、临时密码)。
